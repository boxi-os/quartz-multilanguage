This handbook belongs to the Quartz plugin **quartz-multilanguage**. And it is built
with it: the switcher at the top right comes from there.

- <a href="{{root}}/en/1-getting-started/">Getting started</a> — what Quartz cannot do alone
- <a href="{{root}}/en/2-detection/">Detection</a> — where a page's language comes from
- <a href="{{root}}/en/3-linking/">Linking</a> — how two pages find each other
- <a href="{{root}}/en/9-reference/">Reference</a> — every option on one page

This box, too, appears twice in the configuration — once per language.
